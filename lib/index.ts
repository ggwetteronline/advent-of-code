import './extend-prototypes';
import { lib, Run, recursiveWithMemory } from './lib';
import {
  Direction,
  Point,
  Polygon,
  BaseLocation,
  BaseLocationMap,
  LocationRunner,
  BaseArea,
} from './geometry';

export {
  lib,
  recursiveWithMemory,
  type Run,
  BaseLocation,
  BaseLocationMap,
  LocationRunner,
  Direction,
  BaseArea,
  type Point,
  Polygon,
};
